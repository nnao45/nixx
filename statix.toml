disabled = ["unquoted_uri"]
# shellint test fixtures are intentionally parse-broken nixx (parse-wall ${…}
# forms) — not real Nix, so keep statix off them.
ignore = [".direnv", "tests/shellint-fixtures"]
